# db
$ docker run --name assistant.mongo -p "27017:27017" -d mongo

# backend
$ docker build -t keyfinder .

 docker run -it --rm -v /media/krzysiek/Dane/music/:/collection keyfinder "/collection/Other/2015/05. maj/ANNA - Distractions [Original Mix].mp3"

# web
$ docker build -t php.nginx .

(cd do katalogu projektu)

$ docker run -it -p 8080:80 --name assistant.web --hostname $(hostname) –restart=always --link "assistant.mongo:assistant.mongo" -v $PWD:/data -v /media/krzysiek/Dane/music/:/collection -w /data -d php.nginx

$ docker exec -it assistant.web supervisorctl restart nginx
