{% if issues|length == 0 %}
    <div class="card">
        <div class="card-body">
            <p class="text-muted mb-0">Brak problemów w tej kategorii.</p>
        </div>
    </div>
{% else %}
    <div class="card">
        <div class="card-body">
            <div class="divide-y">
                {% for issue in issues %}
                    <div class="row py-2 align-items-start {{ issue.ignored ? 'text-muted' : '' }}" data-element="analysis-issue">
                        <div class="col">
                            {% if issue.details.track is defined %}
                                {% set highlightFields = [] %}
                                {% if viewType.value == 'empty-metadata' %}
                                    {% set highlightFields = [issue.details.field] %}
                                {% elseif viewType.value == 'suspicious-year' %}
                                    {% set highlightFields = ['year'] %}
                                {% elseif viewType.value == 'low-audio-quality' %}
                                    {% set highlightFields = [] %}
                                    {% if issue.details.is_low_bitrate is defined and issue.details.is_low_bitrate %}
                                        {% set highlightFields = highlightFields|merge(['bitrate']) %}
                                    {% endif %}
                                    {% if issue.details.is_mono is defined and issue.details.is_mono %}
                                        {% set highlightFields = highlightFields|merge(['channel_mode']) %}
                                    {% endif %}
                                {% endif %}

                                {% include '@collection/status/trackinfo.twig' with {
                                    track: issue.details.track,
                                    file: issue.details.file|default({}),
                                    highlight_fields: highlightFields
                                } only %}
                            {% else %}
                                <a class="text-reset" href="{{ url_for('track.track.index', { guid: issue.details.guid }) }}">
                                    {{ issue.details.track_name }}
                                </a>

                                {% if viewType.value == 'low-audio-quality' and issue.details.reason is defined %}
                                    <span class="text-danger ms-1">{{ issue.details.reason }}</span>
                                {% elseif viewType.value == 'empty-metadata' %}
                                    <span class="text-danger ms-1">{{ issue.details.field }}</span>
                                {% elseif viewType.value == 'suspicious-year' %}
                                    <span class="text-danger ms-1">{{ issue.details.value }}</span>
                                {% endif %}
                            {% endif %}
                        </div>
                        <div class="col-auto">
                            {% include '@collection/status/infobutton.twig' with { issue } only %}
                            {% include '@collection/status/ignorebutton.twig' with { issue } only %}
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endif %}
